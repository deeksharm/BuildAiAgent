1. python -m pip install --upgrade pip
   
2. pip install "langgraph>=0.2.0" "langchain>=0.3.0" "langchain-openai>=0.2.0" "python-dotenv>=1.0.0"
3. python -m pip install azure-core azure-search-documents
4. python -m pip install openai
5. python -m pip install python-dotenv
6. python -m pip install python-dotenv

